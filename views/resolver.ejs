<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .container {
            max-width: 1200px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <%- include ('navigation.ejs') %>
    <h2>Resolver work</h2>

    <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Mobile</th>
            <th>Issue</th>
            <th>Note</th>
            <th>File</th>
          </tr>
        </thead>
         <tbody>
           
          <tr>
            <td><%- selectedTicket.name %></td>
            <td><%- selectedTicket.email %></td>
            <td><%- selectedTicket.phone %></td>
            <td><%- selectedTicket.issue %></td>
            <td><%- selectedTicket.note %></td>
            <td><%- selectedTicket.file %></td>
          </tr>
          <% }); %>
        </tbody>
      </table>
</body>
</html>